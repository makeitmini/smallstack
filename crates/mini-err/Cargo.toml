[package]
name = "mini-err"
version = "0.3.8"
edition.workspace = true

[features]
default = []
serde = ["dep:serde"]

[dependencies]
serde = { version = "1", optional = true }

[dev-dependencies]
serde_json = "1"
