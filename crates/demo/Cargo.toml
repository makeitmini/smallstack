[package]
name = "demo"
version = "0.1.0"
edition.workspace = true

[dependencies]
mini-serve = { path = "../mini-serve" }
hyper = "1"
tokio = { version = "1", features = ["rt", "macros", "net", "signal"] }
serde_json = "1"
